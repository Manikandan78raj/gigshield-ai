# Backend API Structure

The backend will be built using Node.js and Express.

Example API Endpoints:

POST /register-worker  
GET /calculate-premium  
POST /activate-policy  
GET /monitor-disruptions  
POST /trigger-claim  
POST /process-payout  

The backend integrates with external APIs such as weather and pollution APIs to monitor disruptions.
